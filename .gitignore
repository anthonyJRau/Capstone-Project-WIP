# Ignore everything
*

# But not these:
!.gitignore
!README.md

# Allow Python source files
!*.py

# Allow files starting with "capstone_"
!capstone_*

# Allow folders so .py files inside them are visible
!*/
